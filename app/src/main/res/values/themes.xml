<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoDayNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DoDayNote" parent="Base.Theme.DoDayNote" />

    <!-- 위젯에 적용할 테마-->
    <style name="Theme.Widget.GreenAccent" parent="android:Theme.Material.Light">
        <item name="colorAccent">@color/main_green</item>
        <item name="android:colorAccent">@color/main_green</item>
    </style>
</resources>